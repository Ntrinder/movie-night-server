import {Recommendation, Room} from "@types";

// add recommendation
export const addRecommendation = (room: Room, newRecommendation: Recommendation, userId: string): void => {

}

// remove recommendation
export const removeRecommendation = (room: Room, newRecommendation: Recommendation): void => {

}

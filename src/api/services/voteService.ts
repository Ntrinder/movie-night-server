import {Recommendation, Room} from "@types";

export const addVote = (room: Room, recommendation: Recommendation, userId: string): void => {
    // find room
    // find recommendation
    // add vote
}

export const removeVote = (room: Room, recommendation: Recommendation, userId: string): void => {

}
